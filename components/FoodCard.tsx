import React from 'react'
import { borderColor } from '../styles/constants'

function FoodCard() {
  return (
    <div className={`w-full border ${borderColor}`}>
      
    </div>
  )
}

export default FoodCard
